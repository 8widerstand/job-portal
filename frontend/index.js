/*fetch('/markdown/stelle1').then(res => res.text()).then(markdown => {    const html = marked.parse(markdown);     document.getElementById('markdown-content').innerHTML = html;}).catch(err => {    document.getElementById('markdown-content').innerHTML = err.toString();    console.log(err);}); */fetch('/json/data')    .then(res => res.json())    .then(data => {        const jobDescription = document.getElementById('job-description');        data.forEach(item => {            const div = document.createElement('div');            div.className = 'card';            div.innerHTML = `<div class="item"> <h3>${item.title}</h3><p>${item.description}</p> </div> <div class="button"><div class="btn"> Mehr Erfahren</div> </div>`;            jobDescription.appendChild(div);        })    }).catch(err => console.log(err));