<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
</head>
<body>
  @each(job in jobs)
    <div>
        <h2>{{ job.title }}</h2>
        <p>{{ job.description }}</p>
        <a href="/jobs/{{ job.id }}">View details</a>
    </div>
  @end
</body>
</html>
