let tagLastName = document.getElementById("lastName");let tagName = document.getElementById("firstName");let uniName = document.getElementById("uni-name");let studyName = document.getElementById("studyName");let mail = document.getElementById("email");let phone = document.getElementById("phone");const form = document.querySelector("form");let span = document.querySelector(".sms")let errorMessages = document.querySelectorAll(".error");function fieldCheck(field) {    const index = Array.from(field.parentNode.parentNode.children).indexOf(field.parentNode);    const errorMessage = errorMessages[index];    if (field.value.trim() === "" || field.value.trim().length <= 2) {        field.classList.add("invalid");        if (errorMessage) {            errorMessage.textContent = "Le contenu de ce champ est trop court ou vide";        }        return false;    } else {        field.classList.remove("invalid");        if (errorMessage) {            errorMessage.textContent = "";        }        return true;    }}function validateEmail (email) {    let emailRegExp = new RegExp("[a-z0-9._-]+@[a-z0-9._-]+\\.[a-z0-9._-]+")    if (emailRegExp.test(email.value)) {        email.classList.remove("invalid");    }else {        email.classList.add("invalid");    }}form.addEventListener("submit", (e) => {    e.preventDefault();    try {        fieldCheck(tagLastName);        fieldCheck(tagName);        fieldCheck(uniName);        fieldCheck(studyName);        fieldCheck(mail);        fieldCheck(phone);    }    catch (error) {        console.log("error in form " + error.message);    }});tagLastName.addEventListener("input", () => {    fieldCheck(tagLastName);})tagName.addEventListener("input", () => {    fieldCheck(tagName);})uniName.addEventListener("input", () => {    fieldCheck(uniName);})studyName.addEventListener("input", () => {    fieldCheck(studyName);})mail.addEventListener("change", () => {    validateEmail(mail);})phone.addEventListener("input", () => {    fieldCheck(phone);})